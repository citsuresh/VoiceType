# ROADMAP (memory pointer)

> Edited deliberately when priorities change — not automatically overwritten each session.

The canonical, user-facing backlog for this project is the root [`ROADMAP.md`](../ROADMAP.md)
(ordered list, items removed + renumbered when shipped). This file intentionally does not
duplicate it — to avoid the two backlogs drifting out of sync, always update the root
`ROADMAP.md` when priorities/plans change, and treat it as the single source of truth for
planned work.
